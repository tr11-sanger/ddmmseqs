/*
~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
    Config file for defining DSL2 per module options and publishing paths
~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
    Available keys to override module options:
        ext.args   = Additional arguments appended to command in module.
        ext.args2  = Second set of arguments appended to command in module (multi-tool modules).
        ext.args3  = Third set of arguments appended to command in module (multi-tool modules).
        ext.prefix = File name prefix for output files.
----------------------------------------------------------------------------------------
*/

process {
    executor = 'slurm'
    
    errorStrategy = 'retry'
    maxRetries    = 0
    maxErrors     = '-1'

    cpus = 1
    memory = 1.GB
    time = '12.0 h'
    queue = 'normal'

    withName: DIAMOND_MAKEDB_FROM_PIPE {
        ext.args = ''
        cpus = 2
        memory = 8.GB
        time = '12.0 h'
        queue = 'normal'
    }

    withName: DIAMOND_BLASTP_TO_MST {
        ext.args = "--id ${(int)(params.diamond_min_seq_identity * 100)} --query-cover ${(int)(params.diamond_min_cov * 100)} --subject-cover ${(int)(params.diamond_min_cov * 100)} --max-target-seqs 0 --sensitive"
        ext.args2 = ''
        cpus = 8
        memory = 8.GB
        time = '12.0 h'
        queue = 'normal'
        // publishDir = [
        //     path: { "${params.outdir}/${meta.collection_id}/cluster/${meta.id}" },
        //     mode: params.publish_dir_mode,
        //     saveAs: { filename ->
        //         return filename
        //     },
        // ]
    }

    withName: MST_TO_CLUSTER {
        cpus = 1
        memory = 32.GB
        time = '48.0 h'
        queue = 'long'
        // publishDir = [
        //     path: { "${params.outdir}/${meta.id}/cluster" },
        //     mode: params.publish_dir_mode,
        //     saveAs: { filename ->
        //         return filename
        //     },
        // ]
    }

    withName: MMSEQS_CREATEDB {
        cpus = 4
        memory = 2.GB
        time = '12.0 h'
        queue = 'normal'
    }

    withName: MMSEQS_LINCLUST {
        ext.args = "-c ${params.mmseqs_min_cov} --min-seq-id ${params.mmseqs_min_seq_identity}"
        cpus = 2
        memory = 8.GB
        time = '12.0 h'
        queue = 'normal'
    }
     
    withName: MMSEQS_CREATETSV {
        cpus = 1
        memory = 4.GB
        time = '12.0 h'
        queue = 'normal'
        // publishDir = [
        //     path: { "${params.outdir}/${meta.id}/mmseqs/linclust" },
        //     mode: params.publish_dir_mode,
        //     saveAs: { filename ->
        //         return filename
        //     },
        // ]
    }

    withName: CONCATENATE {
        cpus = 1
        memory = 1.GB
        time = '12.0 h'
        queue = 'normal'
        publishDir = [
            path: { "${params.outdir}/${meta.id}/mmseqs/linclust" },
            mode: params.publish_dir_mode,
            saveAs: { filename ->
                return "${meta.id}_concatenated.tsv"
            },
        ]
    }
}
