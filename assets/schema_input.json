{
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "$id": "https://raw.githubusercontent.com/ebi-metagenomics/genome-annotation/master/assets/schema_input.json",
    "title": "ebi-metagenomics/rrap pipeline - params.input schema",
    "description": "Schema for the file provided with params.input",
    "type": "array",
    "items": {
        "type": "object",
        "properties": {
            "name": {
                "type": "string",
                "errorMessage": "Sample name must be provided and cannot contain spaces",
                "minLength": 1
            },
            "filelist": {
                "type": "string",
                "errorMessage": "Path for file containing newline-seperated list of faa files must be provided."
            }
        },
        "required": ["name", "filelist"]
    }
}
